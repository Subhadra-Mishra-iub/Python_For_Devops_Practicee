def main():
    folder_paths = input("Enter a list of folder paths separated by spaces:").split()
    print(folder_paths)
    
if __name__ == "__main__":
    main()
    
#Having confusion about when a script is run directly and when it's imported as a module.
#So, writing two examples which are named module_script.py and main_script.py