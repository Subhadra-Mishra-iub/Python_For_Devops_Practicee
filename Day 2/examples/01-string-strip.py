text = "   some spaces game and    lines  "
stripped_text = text.strip()
print("The stripped text:", stripped_text)
# Only takes out the spaces before and after the sentences, not in between extra spaces.
